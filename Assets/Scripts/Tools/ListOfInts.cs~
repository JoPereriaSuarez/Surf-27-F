using System;
using System.Collections.Generic;
using UnityEngine;

public class ListOfInts
{
	public List<int> list
	{
		get; private set;
	}

	public ListOfInts (int number)
	{
		list = new List<int>(number);
		for (int i = 0; i < number; i++)
		{
			list.Add(i);
		}
	}
}

